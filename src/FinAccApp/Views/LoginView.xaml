<UserControl x:Class="FinAccApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#E3F2FD" Offset="0"/>
                <GradientStop Color="#F3E5F5" Offset="0.5"/>
                <GradientStop Color="#E8F5E9" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="420" CornerRadius="16" Style="{DynamicResource GlassCardStyle}" Padding="35,40">
            <StackPanel>
                <Border Width="70" Height="70" CornerRadius="35" Background="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text="شمس" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{StaticResource PersianFont}"/>
                </Border>

                <TextBlock Text="ورود به سامانه مالی شمس" Style="{DynamicResource TitleTextStyle}" HorizontalAlignment="Center" FontSize="22" Margin="0,0,0,5"/>
                <TextBlock Text="مدیریت جامع پروژه‌ها و حسابداری استودیو" Style="{DynamicResource MutedTextStyle}" HorizontalAlignment="Center" FontSize="13" Margin="0,0,0,30"/>

                <TextBlock Text="نام کاربری" Style="{DynamicResource BodyTextStyle}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16"/>

                <TextBlock Text="کلمه عبور" Style="{DynamicResource BodyTextStyle}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <PasswordBox Name="TxtPassword" Margin="0,0,0,16"/>

                <CheckBox Content="مرا به خاطر بسپار" IsChecked="{Binding RememberMe}" Margin="0,0,0,24" HorizontalAlignment="Right"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource DangerBrush}" Style="{DynamicResource MutedTextStyle}" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,16" TextWrapping="Wrap"/>

                <Button Content="ورود به حساب کاربری" Command="{Binding LoginCommand}" Height="45" FontSize="16" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>