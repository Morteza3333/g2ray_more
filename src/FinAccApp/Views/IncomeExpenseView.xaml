<UserControl x:Class="FinAccApp.Views.IncomeExpenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.8*"/>
            <ColumnDefinition Width="2.2*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Style="{DynamicResource GlassCardStyle}" Margin="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" x:Name="TxtListTitle" Text="ریز تراکنش‌های ثبت شده" Style="{DynamicResource TitleTextStyle}" FontSize="15" Margin="5"/>
                <ListBox Grid.Row="1" x:Name="LstTransactions" SelectionChanged="LstTransactions_SelectionChanged" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" Background="{DynamicResource GlassBgBrush}" Margin="2" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding TransDate}" Foreground="{DynamicResource MutedTextBrush}" Style="{DynamicResource MutedTextStyle}" Width="80" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Category}" FontWeight="Bold" Style="{DynamicResource BodyTextStyle}"/>
                                        <TextBlock Text="{Binding Description}" Style="{DynamicResource MutedTextStyle}" MaxWidth="250" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding Amount, StringFormat={}{0:N0} تومان}" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Foreground="{Binding AmountColorBrush}" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Column="0" Style="{DynamicResource GlassCardStyle}" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock x:Name="TxtFormTitle" Text="ثبت تراکنش مالی جدید" Style="{DynamicResource TitleTextStyle}" FontSize="16" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="جدید" Click="BtnNew_Click" Margin="0,0,5,0" Padding="10,5"/>
                        <Button Content="ذخیره" Click="BtnSave_Click" Style="{StaticResource SuccessButtonStyle}" Margin="5,0,5,0" Padding="10,5"/>
                        <Button Content="حذف" Click="BtnDelete_Click" Style="{StaticResource DangerButtonStyle}" Margin="5,0,0,0" Padding="10,5"/>
                    </StackPanel>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="تاریخ شمسی" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtDate"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="مبلغ (تومان)" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtAmount"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="دسته‌بندی" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                        <ComboBox x:Name="CboCategory" Height="36" Margin="0,0,0,12"/>

                        <Grid x:Name="GridProjectRow" Margin="0,0,0,12" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="نام مشتری" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtClient"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="پروژه مربوطه" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <ComboBox x:Name="CboProject" Height="36"/>
                            </StackPanel>
                        </Grid>

                        <CheckBox x:Name="ChkPersonal" Content="این یک تراکنش شخصی مدیر است" Margin="0,0,0,15"/>

                        <TextBlock Text="توضیحات و بابت" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtDescription" Height="70" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,12"/>

                        <TextBlock Text="رسید پیوست یا سند مدرک" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtAttachment" IsReadOnly="True" Style="{DynamicResource MutedTextStyle}" Background="#FAFAFA" Margin="0,0,0,12"/>
                        <Button Content="انتخاب سند پیوست" Click="BtnAttach_Click" Style="{StaticResource AccentButtonStyle}" Height="36"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>