<Window x:Class="FinAccApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="سامانه مالی و حسابداری استودیو شمس"
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBgBrush}"
        FlowDirection="RightToLeft">
    <Grid>
        <ContentControl x:Name="LoginContent" Visibility="Collapsed" Panel.ZIndex="10"/>

        <Grid x:Name="MainWorkspace" Visibility="Visible">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource SidebarBgBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="0,0,1,0" Padding="15,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,25" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Border Width="40" Height="40" CornerRadius="10" Background="{DynamicResource PrimaryBrush}" Margin="0,0,10,0">
                            <TextBlock Text="ش" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding CompanyName}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock Text="نسخه ۱.۰ - فعال" FontSize="11" Foreground="{DynamicResource MutedTextBrush}"/>
                        </StackPanel>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="NavStack" Margin="0,5">
                            <Button Content="داشبورد (خلاصه وضعیت)" Click="Nav_Click" Tag="Dashboard" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="مدیریت پروژه‌ها" Click="Nav_Click" Tag="Projects" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="مدیریت اقساط" Click="Nav_Click" Tag="Installments" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="درآمدهای استودیو" Click="Nav_Click" Tag="Income" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="هزینه‌های استودیو" Click="Nav_Click" Tag="Expenses" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="امور مالی شخصی" Click="Nav_Click" Tag="Personal" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="مدیریت مشتریان" Click="Nav_Click" Tag="Clients" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="صدور فاکتور" Click="Nav_Click" Tag="Invoices" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="گزارشات و نمودارها" Click="Nav_Click" Tag="Reports" Margin="0,3" Height="40" HorizontalContentAlignment="Right" Padding="15,0" Background="Transparent" Foreground="{DynamicResource TextBrush}"/>
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2" Margin="0,15,0,0">
                        <Separator Background="{DynamicResource GlassBorderBrush}" Margin="0,0,0,15"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="حالت تاریک" Click="ToggleTheme_Click" Margin="0,0,5,0" FontSize="11" Padding="5" Background="{DynamicResource GlassBgBrush}" Foreground="{DynamicResource TextBrush}"/>
                            <Button Grid.Column="1" Content="تنظیمات سیستم" Click="Nav_Click" Tag="Settings" Margin="5,0,0,0" FontSize="11" Padding="5" Background="{DynamicResource GlassBgBrush}" Foreground="{DynamicResource TextBrush}"/>
                        </Grid>
                        <Button Content="خروج از سامانه" Click="Logout_Click" Height="36" Background="{DynamicResource DangerBrush}" Foreground="White"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="1" Margin="25,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" x:Name="TxtPageHeader" Text="داشبورد خلاصه حساب‌ها" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>

                    <Grid Grid.Column="1" MaxWidth="400" HorizontalAlignment="Right" Margin="0,0,20,0" Width="300">
                        <TextBox x:Name="TxtGlobalSearch" TextChanged="GlobalSearch_TextChanged" VerticalAlignment="Center" Padding="30,8,10,8"/>
                        <TextBlock IsHitTestVisible="False" Text="جستجو در کل سامانه..." VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0" Foreground="{DynamicResource MutedTextBrush}" FontSize="12">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=TxtGlobalSearch}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="BtnNotifications" Content="🔔 اعلان‌ها (۰)" Click="BtnNotifications_Click" Margin="0,0,10,0" Padding="12,6" Background="{DynamicResource GlassBgBrush}" Foreground="{DynamicResource TextBrush}"/>
                        <Button Content="➕ ثبت سریع پروژه" Click="QuickAddProject_Click" Padding="12,6" Style="{StaticResource SuccessButtonStyle}"/>
                    </StackPanel>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ContentControl x:Name="WorkspaceViewport" Margin="0,0,0,10"/>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>