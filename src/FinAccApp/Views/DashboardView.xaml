<UserControl x:Class="FinAccApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:FinAccApp.Views.Controls"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="1200" d:DesignWidth="900">
    <Grid>
        <StackPanel Margin="0,0,0,20">
            <UniformGrid Columns="4" Rows="2" Margin="0,0,0,20">
                <Border Style="{DynamicResource GlassCardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="کل درآمد ناخالص استودیو" Style="{DynamicResource MutedTextStyle}"/>
                        <TextBlock x:Name="TxtGrossIncome" Text="۰ تومان" Style="{DynamicResource TitleTextStyle}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{DynamicResource GlassCardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="کل هزینه‌های استودیو" Style="{DynamicResource MutedTextStyle}"/>
                        <TextBlock x:Name="TxtTotalExpenses" Text="۰ تومان" Style="{DynamicResource TitleTextStyle}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource DangerBrush}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{DynamicResource GlassCardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="سود خالص استودیو" Style="{DynamicResource MutedTextStyle}"/>
                        <TextBlock x:Name="TxtNetProfit" Text="۰ تومان" Style="{DynamicResource TitleTextStyle}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{DynamicResource GlassCardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="موجودی صندوق نقدی" Style="{DynamicResource MutedTextStyle}"/>
                        <TextBlock x:Name="TxtCashBalance" Text="۰ تومان" Style="{DynamicResource TitleTextStyle}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{DynamicResource GlassCardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="موجودی حساب بانکی" Style="{DynamicResource MutedTextStyle}"/>
                        <TextBlock x:Name="TxtBankBalance" Text="۰ تومان" Style="{DynamicResource TitleTextStyle}" FontSize="18" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{DynamicResource GlassCardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="درآمد شخصی مدیر" Style="{DynamicResource MutedTextStyle}"/>
                        <TextBlock x:Name="TxtPersonalIncome" Text="۰ تومان" Style="{DynamicResource TitleTextStyle}" FontSize="18" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{DynamicResource GlassCardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="پروژه‌های فعال جاری" Style="{DynamicResource MutedTextStyle}"/>
                        <TextBlock x:Name="TxtActiveProjects" Text="۰ پروژه" Style="{DynamicResource TitleTextStyle}" FontSize="18" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{DynamicResource GlassCardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="اقساط معوقه و معلق" Style="{DynamicResource MutedTextStyle}"/>
                        <TextBlock x:Name="TxtPendingInstallments" Text="۰ قسط" Style="{DynamicResource TitleTextStyle}" FontSize="18" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1.2*"/>
                </Grid.ColumnDefinitions>

                <controls:GlassChart Grid.Column="0" x:Name="IncomeExpenseChart" Title="نمودار مقایسه ماهانه درآمد و هزینه (تومان)" Type="Bar" Height="260" Margin="5"/>
                <controls:GlassChart Grid.Column="1" x:Name="ProjectStatusChart" Title="نسبت وضعیت پروژه‌ها" Type="Pie" Height="260" Margin="5"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{DynamicResource GlassCardStyle}" Margin="5" MinHeight="250">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="آخرین فعالیت‌ها و وقایع" Style="{DynamicResource TitleTextStyle}" FontSize="16"/>
                        <ListBox Grid.Row="1" x:Name="LstActivities" Background="Transparent" BorderThickness="0" Margin="0,10,0,0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="0,0,0,1" Padding="0,8" Margin="0,0,0,5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding ShamsiDate}" Foreground="{DynamicResource AccentBrush}" Style="{DynamicResource MutedTextStyle}" Width="80" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Description}" Style="{DynamicResource BodyTextStyle}" TextWrapping="Wrap" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Style="{DynamicResource GlassCardStyle}" Margin="5" MinHeight="250">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="سررسید اقساط پیش‌رو" Style="{DynamicResource TitleTextStyle}" FontSize="16"/>
                        <ListBox Grid.Row="1" x:Name="LstUpcomingInstallments" Background="Transparent" BorderThickness="0" Margin="0,10,0,0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="0,0,0,1" Padding="0,8" Margin="0,0,0,5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding DueDate}" Foreground="{DynamicResource PrimaryBrush}" Style="{DynamicResource MutedTextStyle}" Width="80" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding DisplayTitle}" Style="{DynamicResource BodyTextStyle}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Amount, StringFormat={}{0:N0} تومان}" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>