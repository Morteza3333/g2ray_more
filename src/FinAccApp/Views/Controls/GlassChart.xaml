<UserControl x:Class="FinAccApp.Views.Controls.GlassChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <Border Style="{DynamicResource GlassCardStyle}" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="ChartTitle" Text="نمودار عملکرد" Style="{DynamicResource TitleTextStyle}" Margin="0" FontSize="16"/>
            </StackPanel>

            <Grid Grid.Row="1" x:Name="ChartArea">
                <TextBlock x:Name="EmptyMsg" Text="داده‌ای برای نمایش یافت نشد" Style="{DynamicResource MutedTextStyle}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>

                <Canvas x:Name="BarCanvas" Background="Transparent" Visibility="Collapsed"/>

                <Grid x:Name="PieGrid" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Canvas Grid.Column="0" x:Name="PieCanvas" Background="Transparent"/>
                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="LegendStack" VerticalAlignment="Center" Margin="10,0"/>
                    </ScrollViewer>
                </Grid>

                <Canvas x:Name="LineCanvas" Background="Transparent" Visibility="Collapsed"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>