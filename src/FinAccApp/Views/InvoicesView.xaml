<UserControl x:Class="FinAccApp.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Style="{DynamicResource GlassCardStyle}" Margin="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="فاکتورهای صادر شده پیشین" Style="{DynamicResource TitleTextStyle}" FontSize="15" Margin="5"/>
                <ListBox Grid.Row="1" x:Name="LstInvoices" SelectionChanged="LstInvoices_SelectionChanged" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" Background="{DynamicResource GlassBgBrush}" Margin="2" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding InvoiceNumber, StringFormat=فاکتور شماره {0}}" FontWeight="Bold" Style="{DynamicResource BodyTextStyle}"/>
                                        <TextBlock Text="{Binding ClientName}" Style="{DynamicResource MutedTextStyle}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Total, StringFormat={}{0:N0} تومان}" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"/>
                                        <TextBlock Text="{Binding ShamsiDate}" Style="{DynamicResource MutedTextStyle}" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Column="0" Style="{DynamicResource GlassCardStyle}" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="صدور فاکتور جدید" Style="{DynamicResource TitleTextStyle}" FontSize="16" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="جدید" Click="BtnNew_Click" Margin="0,0,5,0" Padding="10,5"/>
                        <Button Content="ذخیره فاکتور" Click="BtnSave_Click" Style="{StaticResource SuccessButtonStyle}" Margin="5,0,5,0" Padding="10,5"/>
                        <Button Content="خروجی PDF" Click="BtnExportPdf_Click" Style="{StaticResource AccentButtonStyle}" Margin="5,0,0,0" Padding="10,5"/>
                    </StackPanel>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="شماره فاکتور" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtInvoiceNum"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="تاریخ شمسی" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtDate"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="نام کارفرما" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtClient"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="تلفن همراه" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtPhone"/>
                            </StackPanel>
                        </Grid>

                        <Separator Background="{DynamicResource GlassBorderBrush}" Margin="0,5,0,15"/>

                        <TextBlock Text="ریز اقلام و خدمات فاکتور (توضیحات | تعداد | قیمت واحد)" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBlock Text="فرمت ورود: طراحی سایت شمس, 1, 12000000 (هر خط یک آیتم)" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,8"/>
                        <TextBox x:Name="TxtItemsInput" Height="100" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,12" TextChanged="CalculateInvoiceTotals"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="تخفیف نقدی (تومان)" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtDiscount" TextChanged="CalculateInvoiceTotals"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="درصد مالیات ارزش افزوده" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtTaxPercent" TextChanged="CalculateInvoiceTotals" Text="9"/>
                            </StackPanel>
                        </Grid>

                        <Border Style="{DynamicResource GlassCardStyle}" Margin="0,10,0,0" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="مجموع نهایی فاکتور:" Style="{DynamicResource MutedTextStyle}"/>
                                    <TextBlock x:Name="LblFinalTotal" Text="۰ تومان" Style="{DynamicResource TitleTextStyle}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Margin="0,5,0,0"/>
                                </StackPanel>

                                <Border Grid.Column="1" Width="80" Height="80" CornerRadius="8" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1.5" Background="White" Padding="5" VerticalAlignment="Center">
                                    <Grid>
                                        <Viewbox Stretch="Uniform">
                                            <Canvas Width="100" Height="100" Background="White">
                                                <Rectangle Width="30" Height="30" Fill="Black" Canvas.Left="0" Canvas.Top="0"/>
                                                <Rectangle Width="14" Height="14" Fill="White" Canvas.Left="8" Canvas.Top="8"/>
                                                <Rectangle Width="30" Height="30" Fill="Black" Canvas.Left="70" Canvas.Top="0"/>
                                                <Rectangle Width="14" Height="14" Fill="White" Canvas.Left="78" Canvas.Top="8"/>
                                                <Rectangle Width="30" Height="30" Fill="Black" Canvas.Left="0" Canvas.Top="70"/>
                                                <Rectangle Width="14" Height="14" Fill="White" Canvas.Left="8" Canvas.Top="78"/>
                                                <Rectangle Width="10" Height="10" Fill="Black" Canvas.Left="45" Canvas.Top="45"/>
                                                <Rectangle Width="10" Height="10" Fill="Black" Canvas.Left="35" Canvas.Top="15"/>
                                                <Rectangle Width="10" Height="10" Fill="Black" Canvas.Left="15" Canvas.Top="35"/>
                                                <Rectangle Width="10" Height="10" Fill="Black" Canvas.Left="55" Canvas.Top="75"/>
                                                <Rectangle Width="10" Height="10" Fill="Black" Canvas.Left="75" Canvas.Top="55"/>
                                            </Canvas>
                                        </Viewbox>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>