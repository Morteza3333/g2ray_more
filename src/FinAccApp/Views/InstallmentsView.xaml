<UserControl x:Class="FinAccApp.Views.InstallmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Style="{DynamicResource GlassCardStyle}" Margin="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="انتخاب پروژه جهت کنترل اقساط" Style="{DynamicResource TitleTextStyle}" FontSize="15" Margin="5"/>
                <ListBox Grid.Row="1" x:Name="LstProjects" SelectionChanged="LstProjects_SelectionChanged" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" Background="{DynamicResource GlassBgBrush}" Margin="2" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding ProjectTitle}" FontWeight="Bold" Style="{DynamicResource BodyTextStyle}"/>
                                        <TextBlock Text="{Binding ClientName}" Style="{DynamicResource MutedTextStyle}"/>
                                    </StackPanel>
                                    <Border Grid.Column="1" Background="{Binding StatusColorBrush}" CornerRadius="12" Padding="8,4" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Column="0" Style="{DynamicResource GlassCardStyle}" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="کنترل سه‌مرحله‌ای پرداخت اقساط" Style="{DynamicResource TitleTextStyle}" FontSize="16"/>
                    <TextBlock x:Name="TxtSelectedProjectTitle" Text="لطفا یک پروژه از لیست روبرو انتخاب نمایید." FontWeight="Bold" FontSize="15" Foreground="{DynamicResource PrimaryBrush}" Margin="0,5,0,0"/>
                </StackPanel>

                <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,20">
                    <Border Style="{DynamicResource GlassCardStyle}" Margin="3" Padding="10">
                        <StackPanel>
                            <TextBlock Text="کل مبلغ پروژه" Style="{DynamicResource MutedTextStyle}" FontSize="11"/>
                            <TextBlock x:Name="TxtTotalAmount" Text="۰ تومان" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{DynamicResource GlassCardStyle}" Margin="3" Padding="10">
                        <StackPanel>
                            <TextBlock Text="دریافت شده" Style="{DynamicResource MutedTextStyle}" FontSize="11"/>
                            <TextBlock x:Name="TxtReceivedAmount" Text="۰ تومان" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{DynamicResource GlassCardStyle}" Margin="3" Padding="10">
                        <StackPanel>
                            <TextBlock Text="باقی‌مانده" Style="{DynamicResource MutedTextStyle}" FontSize="11"/>
                            <TextBlock x:Name="TxtRemainingAmount" Text="۰ تومان" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Foreground="{DynamicResource DangerBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="InstallmentEditorStack" IsEnabled="False">
                        <TextBlock Text="درصد پیشرفت و تسویه مالی" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,20">
                            <ProgressBar x:Name="PrgProgress" Height="22" Minimum="0" Maximum="100" Value="0" Foreground="{DynamicResource SuccessBrush}"/>
                            <TextBlock x:Name="TxtPercentage" Text="۰٪" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="12" Foreground="Black"/>
                        </Grid>

                        <Separator Background="{DynamicResource GlassBorderBrush}" Margin="0,0,0,15"/>

                        <Border BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12" Background="#0A000000">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="مرحله اول - پیش‌پرداخت" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Margin="0,0,0,8"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                            <TextBlock Text="مبلغ (تومان)" Style="{DynamicResource MutedTextStyle}" FontSize="10" Margin="0,0,0,4"/>
                                            <TextBox x:Name="TxtInst1Amount" TextChanged="Installment_ValuesChanged"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                            <TextBlock Text="سررسید شمسی" Style="{DynamicResource MutedTextStyle}" FontSize="10" Margin="0,0,0,4"/>
                                            <TextBox x:Name="TxtInst1DueDate"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                                <CheckBox Grid.Column="1" x:Name="ChkInst1Paid" Content="پرداخت شده" VerticalAlignment="Center" Margin="15,0,0,0" Checked="Installment_ValuesChanged" Unchecked="Installment_ValuesChanged"/>
                            </Grid>
                        </Border>

                        <Border BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12" Background="#0A000000">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="مرحله دوم - اواسط پروژه" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Margin="0,0,0,8"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                            <TextBlock Text="مبلغ (تومان)" Style="{DynamicResource MutedTextStyle}" FontSize="10" Margin="0,0,0,4"/>
                                            <TextBox x:Name="TxtInst2Amount" TextChanged="Installment_ValuesChanged"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                            <TextBlock Text="سررسید شمسی" Style="{DynamicResource MutedTextStyle}" FontSize="10" Margin="0,0,0,4"/>
                                            <TextBox x:Name="TxtInst2DueDate"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                                <CheckBox Grid.Column="1" x:Name="ChkInst2Paid" Content="پرداخت شده" VerticalAlignment="Center" Margin="15,0,0,0" Checked="Installment_ValuesChanged" Unchecked="Installment_ValuesChanged"/>
                            </Grid>
                        </Border>

                        <Border BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12" Background="#0A000000">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="مرحله سوم - تسویه نهایی" Style="{DynamicResource BodyTextStyle}" FontWeight="Bold" Margin="0,0,0,8"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                            <TextBlock Text="مبلغ (تومان)" Style="{DynamicResource MutedTextStyle}" FontSize="10" Margin="0,0,0,4"/>
                                            <TextBox x:Name="TxtInst3Amount" TextChanged="Installment_ValuesChanged"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                            <TextBlock Text="سررسید شمسی" Style="{DynamicResource MutedTextStyle}" FontSize="10" Margin="0,0,0,4"/>
                                            <TextBox x:Name="TxtInst3DueDate"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                                <CheckBox Grid.Column="1" x:Name="ChkInst3Paid" Content="پرداخت شده" VerticalAlignment="Center" Margin="15,0,0,0" Checked="Installment_ValuesChanged" Unchecked="Installment_ValuesChanged"/>
                            </Grid>
                        </Border>

                        <Button Content="بروزرسانی و ثبت نهایی اقساط" Click="BtnSaveInstallments_Click" Style="{StaticResource SuccessButtonStyle}" Height="40" Margin="0,10,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>