<UserControl x:Class="FinAccApp.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.8*"/>
            <ColumnDefinition Width="2.2*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Style="{DynamicResource GlassCardStyle}" Margin="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="لیست کارفرمایان و مشتریان" Style="{DynamicResource TitleTextStyle}" FontSize="15" Margin="5"/>
                <ListBox Grid.Row="1" x:Name="LstClients" SelectionChanged="LstClients_SelectionChanged" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" Background="{DynamicResource GlassBgBrush}" Margin="2" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ClientName}" FontWeight="Bold" Style="{DynamicResource BodyTextStyle}"/>
                                        <TextBlock Text="{Binding Company}" Style="{DynamicResource MutedTextStyle}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding TotalPaid, StringFormat={}{0:N0} تومان پرداختی}" Style="{DynamicResource BodyTextStyle}" Foreground="{DynamicResource SuccessBrush}"/>
                                        <TextBlock Text="{Binding RemainingBalance, StringFormat={}{0:N0} مانده}" Style="{DynamicResource MutedTextStyle}" Foreground="{DynamicResource DangerBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Column="0" Style="{DynamicResource GlassCardStyle}" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="مشخصات و اطلاعات مشتری" Style="{DynamicResource TitleTextStyle}" FontSize="16" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="جدید" Click="BtnNew_Click" Margin="0,0,5,0" Padding="10,5"/>
                        <Button Content="ذخیره" Click="BtnSave_Click" Style="{StaticResource SuccessButtonStyle}" Margin="5,0,5,0" Padding="10,5"/>
                        <Button Content="حذف" Click="BtnDelete_Click" Style="{StaticResource DangerButtonStyle}" Margin="5,0,0,0" Padding="10,5"/>
                    </StackPanel>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="نام کامل مشتری" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtClientName"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="تلفن تماس" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtPhone"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="آدرس ایمیل" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtEmail"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="نام کمپانی / برند" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtCompany"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="کل مبالغ پرداختی (تومان)" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtPaid"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="کل مانده حساب بدهی (تومان)" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtRemaining"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="آدرس کارفرما" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtAddress" Margin="0,0,0,12"/>

                        <TextBlock Text="یادداشت‌های اختصاصی" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtNotes" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,12"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>